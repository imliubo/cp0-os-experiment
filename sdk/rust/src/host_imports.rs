// Generated by scripts/generate-sdk-bindings.mjs. Do not edit.

#[rustfmt::skip]
#[cfg(target_arch = "wasm32")]
mod target {
    #[link(wasm_import_module = "cardputerzero")]
    unsafe extern "C" {
        #[link_name = "cp0_monotonic_milliseconds"]
        fn raw_cp0_monotonic_milliseconds() -> u64;
        #[link_name = "cp0_wait_event"]
        fn raw_cp0_wait_event(timeout_milliseconds: i32) -> i32;
        #[link_name = "cp0_display_dimensions"]
        fn raw_cp0_display_dimensions() -> u32;
        #[link_name = "cp0_present_rgb565"]
        fn raw_cp0_present_rgb565(pixels: *const u8, pixel_bytes: u32, damage: *const crate::display::Rect, damage_bytes: u32) -> i32;
        #[link_name = "cp0_poll_key_event"]
        fn raw_cp0_poll_key_event(event: *mut u8, event_bytes: u32, timeout_milliseconds: i32) -> i32;
        #[link_name = "cp0_post_notification"]
        fn raw_cp0_post_notification(title: *const u8, title_length: u32, body: *const u8, body_length: u32) -> i32;
        #[link_name = "cp0_http_get"]
        fn raw_cp0_http_get(url: *const u8, url_length: u32, body: *mut u8, body_capacity: u32) -> i64;
        #[link_name = "cp0_document_open"]
        fn raw_cp0_document_open() -> i64;
        #[link_name = "cp0_document_read"]
        fn raw_cp0_document_read(handle: i32, offset: u64, buffer: *mut u8, capacity: u32) -> i64;
        #[link_name = "cp0_document_close"]
        fn raw_cp0_document_close(handle: i32) -> i32;
        #[link_name = "cp0_audio_play_pcm_s16le"]
        fn raw_cp0_audio_play_pcm_s16le(samples: *const u8, sample_bytes: u32) -> i32;
        #[link_name = "cp0_audio_capture_pcm_s16le"]
        fn raw_cp0_audio_capture_pcm_s16le(samples: *mut u8, sample_capacity: u32) -> i32;
        #[link_name = "cp0_camera_capture_rgb565"]
        fn raw_cp0_camera_capture_rgb565(pixels: *mut u8, pixel_bytes: u32) -> i32;
        #[link_name = "cp0_gpio_read"]
        fn raw_cp0_gpio_read(line: u32) -> i32;
        #[link_name = "cp0_gpio_write"]
        fn raw_cp0_gpio_write(line: u32, value: u32) -> i32;
        #[link_name = "cp0_lora_send"]
        fn raw_cp0_lora_send(payload: *const u8, payload_length: u32) -> i32;
        #[link_name = "cp0_lora_receive"]
        fn raw_cp0_lora_receive(payload: *mut u8, payload_capacity: u32, metadata: *mut u8, metadata_bytes: u32, timeout_ms: u32) -> i32;
        #[link_name = "cp0_storage_put"]
        fn raw_cp0_storage_put(key: *const u8, key_length: u32, value: *const u8, value_length: u32) -> i32;
        #[link_name = "cp0_storage_get"]
        fn raw_cp0_storage_get(key: *const u8, key_length: u32, value: *mut u8, value_capacity: u32) -> i32;
        #[link_name = "cp0_storage_delete"]
        fn raw_cp0_storage_delete(key: *const u8, key_length: u32) -> i32;
        #[link_name = "cp0_photos_put"]
        fn raw_cp0_photos_put(key: *const u8, key_length: u32, value: *const u8, value_length: u32) -> i32;
        #[link_name = "cp0_photos_get"]
        fn raw_cp0_photos_get(key: *const u8, key_length: u32, value: *mut u8, value_capacity: u32) -> i32;
        #[link_name = "cp0_photos_index_get"]
        fn raw_cp0_photos_index_get(value: *mut u8, value_capacity: u32) -> i32;
        #[link_name = "cp0_photos_delete"]
        fn raw_cp0_photos_delete(key: *const u8, key_length: u32) -> i32;
        #[link_name = "cp0_intent_send"]
        fn raw_cp0_intent_send(action: *const u8, action_length: u32, payload: *const u8, payload_length: u32) -> i32;
        #[link_name = "cp0_intent_take"]
        fn raw_cp0_intent_take(action: *mut u8, action_capacity: u32, payload: *mut u8, payload_capacity: u32) -> i64;
        #[link_name = "cp0_media_session_update"]
        fn raw_cp0_media_session_update(state: u32, supported_actions: u32) -> i32;
        #[link_name = "cp0_media_take_action"]
        fn raw_cp0_media_take_action() -> i32;
    }

    pub(crate) fn cp0_monotonic_milliseconds() -> u64 {
        unsafe { raw_cp0_monotonic_milliseconds() }
    }

    pub(crate) fn cp0_wait_event(timeout_milliseconds: i32) -> i32 {
        unsafe { raw_cp0_wait_event(timeout_milliseconds) }
    }

    pub(crate) fn cp0_display_dimensions() -> u32 {
        unsafe { raw_cp0_display_dimensions() }
    }

    pub(crate) fn cp0_present_rgb565(pixels: *const u8, pixel_bytes: u32, damage: *const crate::display::Rect, damage_bytes: u32) -> i32 {
        unsafe { raw_cp0_present_rgb565(pixels, pixel_bytes, damage, damage_bytes) }
    }

    pub(crate) fn cp0_poll_key_event(event: *mut u8, event_bytes: u32, timeout_milliseconds: i32) -> i32 {
        unsafe { raw_cp0_poll_key_event(event, event_bytes, timeout_milliseconds) }
    }

    pub(crate) fn cp0_post_notification(title: *const u8, title_length: u32, body: *const u8, body_length: u32) -> i32 {
        unsafe { raw_cp0_post_notification(title, title_length, body, body_length) }
    }

    pub(crate) fn cp0_http_get(url: *const u8, url_length: u32, body: *mut u8, body_capacity: u32) -> i64 {
        unsafe { raw_cp0_http_get(url, url_length, body, body_capacity) }
    }

    pub(crate) fn cp0_document_open() -> i64 {
        unsafe { raw_cp0_document_open() }
    }

    pub(crate) fn cp0_document_read(handle: i32, offset: u64, buffer: *mut u8, capacity: u32) -> i64 {
        unsafe { raw_cp0_document_read(handle, offset, buffer, capacity) }
    }

    pub(crate) fn cp0_document_close(handle: i32) -> i32 {
        unsafe { raw_cp0_document_close(handle) }
    }

    pub(crate) fn cp0_audio_play_pcm_s16le(samples: *const u8, sample_bytes: u32) -> i32 {
        unsafe { raw_cp0_audio_play_pcm_s16le(samples, sample_bytes) }
    }

    pub(crate) fn cp0_audio_capture_pcm_s16le(samples: *mut u8, sample_capacity: u32) -> i32 {
        unsafe { raw_cp0_audio_capture_pcm_s16le(samples, sample_capacity) }
    }

    pub(crate) fn cp0_camera_capture_rgb565(pixels: *mut u8, pixel_bytes: u32) -> i32 {
        unsafe { raw_cp0_camera_capture_rgb565(pixels, pixel_bytes) }
    }

    pub(crate) fn cp0_gpio_read(line: u32) -> i32 {
        unsafe { raw_cp0_gpio_read(line) }
    }

    pub(crate) fn cp0_gpio_write(line: u32, value: u32) -> i32 {
        unsafe { raw_cp0_gpio_write(line, value) }
    }

    pub(crate) fn cp0_lora_send(payload: *const u8, payload_length: u32) -> i32 {
        unsafe { raw_cp0_lora_send(payload, payload_length) }
    }

    pub(crate) fn cp0_lora_receive(payload: *mut u8, payload_capacity: u32, metadata: *mut u8, metadata_bytes: u32, timeout_ms: u32) -> i32 {
        unsafe { raw_cp0_lora_receive(payload, payload_capacity, metadata, metadata_bytes, timeout_ms) }
    }

    pub(crate) fn cp0_storage_put(key: *const u8, key_length: u32, value: *const u8, value_length: u32) -> i32 {
        unsafe { raw_cp0_storage_put(key, key_length, value, value_length) }
    }

    pub(crate) fn cp0_storage_get(key: *const u8, key_length: u32, value: *mut u8, value_capacity: u32) -> i32 {
        unsafe { raw_cp0_storage_get(key, key_length, value, value_capacity) }
    }

    pub(crate) fn cp0_storage_delete(key: *const u8, key_length: u32) -> i32 {
        unsafe { raw_cp0_storage_delete(key, key_length) }
    }

    pub(crate) fn cp0_photos_put(key: *const u8, key_length: u32, value: *const u8, value_length: u32) -> i32 {
        unsafe { raw_cp0_photos_put(key, key_length, value, value_length) }
    }

    pub(crate) fn cp0_photos_get(key: *const u8, key_length: u32, value: *mut u8, value_capacity: u32) -> i32 {
        unsafe { raw_cp0_photos_get(key, key_length, value, value_capacity) }
    }

    pub(crate) fn cp0_photos_index_get(value: *mut u8, value_capacity: u32) -> i32 {
        unsafe { raw_cp0_photos_index_get(value, value_capacity) }
    }

    pub(crate) fn cp0_photos_delete(key: *const u8, key_length: u32) -> i32 {
        unsafe { raw_cp0_photos_delete(key, key_length) }
    }

    pub(crate) fn cp0_intent_send(action: *const u8, action_length: u32, payload: *const u8, payload_length: u32) -> i32 {
        unsafe { raw_cp0_intent_send(action, action_length, payload, payload_length) }
    }

    pub(crate) fn cp0_intent_take(action: *mut u8, action_capacity: u32, payload: *mut u8, payload_capacity: u32) -> i64 {
        unsafe { raw_cp0_intent_take(action, action_capacity, payload, payload_capacity) }
    }

    pub(crate) fn cp0_media_session_update(state: u32, supported_actions: u32) -> i32 {
        unsafe { raw_cp0_media_session_update(state, supported_actions) }
    }

    pub(crate) fn cp0_media_take_action() -> i32 {
        unsafe { raw_cp0_media_take_action() }
    }

}

#[rustfmt::skip]
#[cfg(not(target_arch = "wasm32"))]
mod target {
    pub(crate) const fn cp0_monotonic_milliseconds() -> u64 {
        0
    }

    pub(crate) const fn cp0_wait_event(_timeout_milliseconds: i32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_display_dimensions() -> u32 {
        9830720
    }

    pub(crate) const fn cp0_present_rgb565(_pixels: *const u8, _pixel_bytes: u32, _damage: *const crate::display::Rect, _damage_bytes: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_poll_key_event(_event: *mut u8, _event_bytes: u32, _timeout_milliseconds: i32) -> i32 {
        0
    }

    pub(crate) const fn cp0_post_notification(_title: *const u8, _title_length: u32, _body: *const u8, _body_length: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_http_get(_url: *const u8, _url_length: u32, _body: *mut u8, _body_capacity: u32) -> i64 {
        -2
    }

    pub(crate) const fn cp0_document_open() -> i64 {
        -2
    }

    pub(crate) const fn cp0_document_read(_handle: i32, _offset: u64, _buffer: *mut u8, _capacity: u32) -> i64 {
        -2
    }

    pub(crate) const fn cp0_document_close(_handle: i32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_audio_play_pcm_s16le(_samples: *const u8, _sample_bytes: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_audio_capture_pcm_s16le(_samples: *mut u8, _sample_capacity: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_camera_capture_rgb565(_pixels: *mut u8, _pixel_bytes: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_gpio_read(_line: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_gpio_write(_line: u32, _value: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_lora_send(_payload: *const u8, _payload_length: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_lora_receive(_payload: *mut u8, _payload_capacity: u32, _metadata: *mut u8, _metadata_bytes: u32, _timeout_ms: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_storage_put(_key: *const u8, _key_length: u32, _value: *const u8, _value_length: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_storage_get(_key: *const u8, _key_length: u32, _value: *mut u8, _value_capacity: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_storage_delete(_key: *const u8, _key_length: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_photos_put(_key: *const u8, _key_length: u32, _value: *const u8, _value_length: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_photos_get(_key: *const u8, _key_length: u32, _value: *mut u8, _value_capacity: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_photos_index_get(_value: *mut u8, _value_capacity: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_photos_delete(_key: *const u8, _key_length: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_intent_send(_action: *const u8, _action_length: u32, _payload: *const u8, _payload_length: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_intent_take(_action: *mut u8, _action_capacity: u32, _payload: *mut u8, _payload_capacity: u32) -> i64 {
        -2
    }

    pub(crate) const fn cp0_media_session_update(_state: u32, _supported_actions: u32) -> i32 {
        -2
    }

    pub(crate) const fn cp0_media_take_action() -> i32 {
        -2
    }

}

pub(crate) use target::*;
