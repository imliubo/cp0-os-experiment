[package]
name = "cp0-networkd"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
cp0-network-protocol = { path = "../cp0-network-protocol" }
libc.workspace = true
ureq = { version = "=3.3.0", default-features = false, features = ["rustls"] }
